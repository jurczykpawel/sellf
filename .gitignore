# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor / IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (NEVER commit real credentials)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Private keys and certificates (NEVER commit!)
*.pem
*.key

# Node modules
node_modules/

# Next.js build outputs
.next/
dist/*.map
build/

# Temporary files
*.tmp
*.temp

# Build artifacts
*.tsbuildinfo

# Personal configuration with real credentials
config.js

# PM2 local configuration
ecosystem.config.js

# Gemini temporary files
.gemini-clipboard/

# Claude AI local configuration
.claude/

# Agent implementation plans / briefs — keep out of public repo.
# CLAUDE.md / AGENTS.md stay tracked; these dirs hold task-specific
# REQUIRED-SUB-SKILL briefs that reference private vault paths.
docs/superpowers/
**/docs/superpowers/
docs/plans/
**/docs/plans/

# Sellf license private key (NEVER commit!)
scripts/.sellf-private-key.pem

# Bruno environments (may contain API keys)
bruno/environments/*.bru
!bruno/environments/*.bru.example

# Copilot instructions (IDE-specific)
.copilot-codeGeneration-instructions.md

# Local dev server config
Caddyfile

# Private / internal docs (not for public repo)
priv/

# Local build artifacts
release/
sellf-build.tar.gz
